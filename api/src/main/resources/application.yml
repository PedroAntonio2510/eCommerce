logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: "health"
---
spring:
  config:
    activate:
      on-profile: production
  jpa:
    show-sql: true